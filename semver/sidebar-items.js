initSidebarItems({"enum":[["Identifier","An identifier in the pre-release or build metadata."],["ReqParseError","A `ReqParseError` is returned from methods which parse a string into a `VersionReq`. Each enumeration is one of the possible errors that can occur."]],"struct":[["Version","Represents a version number conforming to the semantic versioning scheme."],["VersionReq","A `VersionReq` is a struct containing a list of predicates that can apply to ranges of version numbers. Matching operations can then be done with the `VersionReq` against a particular version to see if it satisfies some or all of the constraints."]]});